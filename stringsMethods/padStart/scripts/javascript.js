// Add characters before string

let reverseNumber = "0";

console.log(reverseNumber.padStart(5, "1"));

let numbers = ["1", "2", "3", "4", "5"];

numbers.forEach((item) => {
  console.log(item.padStart(2, "0"));
});
